import floatingNav from './blocks/floating-nav';
import marisaFact from './blocks/marisa-fact';
import enableTranscriptToggle from './blocks/transcript.js';

/*
 * Enable transcript toggling.
 */
setTimeout(() => {
  enableTranscriptToggle('transcript--collapsible', true);
}, 1000);
